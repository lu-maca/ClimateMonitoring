package uni.climatemonitor.graphics;
import javax.swing.*;

public class MainWindow extends JFrame {
    JFrame w = new JFrame();
    public MainWindow() {
        w.setSize(400,500);
        w.setLayout(null);
        w.setVisible(true);
    }
}
